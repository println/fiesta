{
  "name": "fiesta-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-force-exit tests/plugins/*.test.mjs",
    "e2e": "node --test --test-concurrency=1 --test-force-exit tests/e2e/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
