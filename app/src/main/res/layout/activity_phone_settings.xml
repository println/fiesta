<?xml version="1.0" encoding="utf-8"?>
<!-- Plano 14, Etapa 8. FrameLayout no lugar de <fragment>: uma tag <fragment>
     declarada em XML nao pode ser substituida em runtime, e cada nivel do
     indice de topicos precisa trocar o fragment (mesma Activity, empilhada). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settings_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />
